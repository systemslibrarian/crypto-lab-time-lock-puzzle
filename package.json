{
  "name": "crypto-lab-time-lock-puzzle",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:a11y": "playwright test",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.61.1",
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.0",
    "vite": "^8.2.0",
    "vitest": "^1.6.0"
  }
}
